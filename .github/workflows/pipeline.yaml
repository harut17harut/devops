#---------------------------------

# Github action push ot docker hub

#---------------------------------

name: devops
on:
  push:
    branches:
      - master

jobs:
  deployment:
    runs-on: ubuntu-latest
    steps:
      -uses: actions/checkout@v1
      -name: test
      -run: echo sayakirim
  zazaza:
